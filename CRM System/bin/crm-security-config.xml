
<beans:beans xmlns="http://www.springframework.org/schema/security" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:oauth2="http://www.springframework.org/schema/security/oauth2"
	 
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans 
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security 
        http://www.springframework.org/schema/security/spring-security.xsd
        http://www.springframework.org/schema/security/oauth2 
        http://www.springframework.org/schema/security/spring-security-oauth2.xsd">
	
	
	<!--  setup for the authorization and token endpoints /oauth/authorize and /oauth/token -->
	<oauth2:authorization-server client-details-service-ref="clientDetailsService"
								 user-approval-handler-ref="userApprovalHandler">
								 
		<oauth2:authorization-code/>
		<oauth2:client-credentials/>
		
	</oauth2:authorization-server>

	<beans:bean id="clientDetailsUserService" class="org.springframework.security.oauth2.provider.client.ClientDetailsUserDetailsService">
		<beans:constructor-arg ref="clientDetailsService"/>
	</beans:bean>

	<oauth2:client-details-service id="clientDetailsService">
	
		<oauth2:client client-id="mailmonkey"
					   authorized-grant-types="authorization_code"
					   authorities="ROLE_CLIENT"
					   scope="read,write"
					   secret="somesecretkey"/>
	
		<oauth2:client client-id="trusted"
					   authorized-grant-types="client_credentials"
					   authorities="ROLE_TRUSTED"
					   scope="trusted"
					   secret="trustedsecret"/>

	</oauth2:client-details-service>	
	
 	<!--  This is a basic security set up, currently for the web-tier only -->
 	
	<!--  a second manager for the electronic clients ONLY -->
	<authentication-manager id="oauthTokenEndpointAuthManager">
		<authentication-provider user-service-ref="clientDetailsUserService"/>	
	</authentication-manager>
	
	<!-- OAuth helper beans -->
	<beans:bean id="oauthTokenEntry" class="org.springframework.security.oauth2.provider.error.OAuth2AuthenticationEntryPoint"/>
	<oauth2:resource-server id="oauthFilter"/>
	<beans:bean id="expressionHandler" class="org.springframework.security.oauth2.provider.expression.OAuth2WebSecurityExpressionHandler"/>

	<!--  the settings for our REST service -->
	<http pattern="/rest/**" create-session="stateless" entry-point-ref="oauthTokenEntry">
		<intercept-url pattern="/rest/customers" method="POST" access="#oauth2.hasScope('write') or #oauth2.hasScope('trusted')" />		
		<intercept-url pattern="/rest/**" access="#oauth2.hasScope('read') or #oauth2.hasScope('write') or #oauth2.hasScope('trusted')"/>	
		<custom-filter ref="oauthFilter" before="PRE_AUTH_FILTER"/>
		<expression-handler ref="expressionHandler"/>
		
		<csrf disabled="true"/>		
	</http>
	
	
	<!-- specific settings for the token endpoint -->
	<http pattern="/oauth/token" create-session="stateless" authentication-manager-ref="oauthTokenEndpointAuthManager">
	
		<intercept-url pattern="/oauth/token" access="hasRole('ROLE_CLIENT') or hasRole('ROLE_TRUSTED')"/>
		<http-basic/>
		<csrf disabled="true"/>
	
	</http>
	
	<!--  this is the vanilla website settings...-->
	<!-- Plugin CAS Authentication via Entrypoint -->
	<http pattern="/**" entry-point-ref="casEntrypoint">	
		<intercept-url pattern="/oauth/**" access="hasRole('ROLE_CRM_USER')"/>
		<intercept-url pattern="/login.jsp" access="permitAll()"/>
		<intercept-url pattern="/css/**" access="permitAll()"/>		
		<intercept-url pattern="/login" access="permitAll()"/>
		<intercept-url pattern="/**" access="hasRole('ROLE_CRM_USER')"/>
		
		<!-- This filter is needed to validate the CAS Generated Tickets -->
		<custom-filter ref="casAuthenticationFilter" before="CAS_FILTER" />
		<csrf/> 		
	</http>
	
	<beans:bean id="casEntrypoint" class="org.springframework.security.cas.web.CasAuthenticationEntryPoint">
		<!-- This url is for CAS authentication -->
		<beans:property name="loginUrl" value="http://localhost:8080/cas/login" />
		<!-- This is the redirect url for cas after authentication -->
		<beans:property name="serviceProperties" ref="serviceProperties" />
	</beans:bean>
	
	<beans:bean id="serviceProperties" class="org.springframework.security.cas.ServiceProperties">
		<beans:property name="service" value="http://localhost:8080/crm/login/cas" />
	</beans:bean>
	
	<beans:bean id="casAuthenticationFilter" class="org.springframework.security.cas.web.CasAuthenticationFilter">
		<beans:property name="authenticationManager" ref="authenticationManager"/>
	</beans:bean>
	
	<!--  configuration specifically to handle user approval -->
	<beans:bean id="approvalStore" class="org.springframework.security.oauth2.provider.approval.InMemoryApprovalStore"/>
	<beans:bean id="userApprovalHandler" class="org.springframework.security.oauth2.provider.approval.ApprovalStoreUserApprovalHandler" autowire="byType"/>
	
	<!--  For this course, we will use in memory authentication - see module 1 of Spring Security Course for details on JDBC providers -->
	<authentication-manager alias="authenticationManager">
		<authentication-provider ref="casAuthenticationProvider">
		</authentication-provider>
	</authentication-manager>
	
	<beans:bean id="casAuthenticationProvider" class="org.springframework.security.cas.authentication.CasAuthenticationProvider">
		<beans:property name="ticketValidator" ref="ticketValidator"/>
		<beans:property name="serviceProperties" ref="serviceProperties" />
		<!-- unique key for application -->
		<beans:property name="key" value="crmkey"/>
		<!-- This is required so that spring framework knows what roles
			 that CAS authenticated user has so that it can give access(authorize)
			 resource-->
		<beans:property name="authenticationUserDetailsService" ref="authoritiesFromCasAttributes"/>
	</beans:bean>
	
	<beans:bean id="authoritiesFromCasAttributes" class="org.springframework.security.cas.userdetails.GrantedAuthorityFromAssertionAttributesUserDetailsService">
		<beans:constructor-arg>
			<beans:array>
				<beans:value>role</beans:value>
			</beans:array>
		</beans:constructor-arg>
	</beans:bean>
	
	<beans:bean id="ticketValidator" class="org.jasig.cas.client.validation.Cas30ServiceTicketValidator">
		<!-- This is url where CAS is running -->
		<beans:constructor-arg value="http://localhost:8080/cas" />
	</beans:bean>
</beans:beans>	